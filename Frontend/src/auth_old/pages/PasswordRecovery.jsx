import { useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { AuthStage, BackButton, Field, SubmitButton, SuccessMark } from '../components/AuthUI'
import { validateIdentity, validatePassword } from '../utils/validation'

export function ForgotPassword(){const navigate=useNavigate();const[email,setEmail]=useState('');const[error,setError]=useState('');const submit=e=>{e.preventDefault();const next=validateIdentity(email);setError(next);if(!next)navigate('/reset-password')};return <AuthStage><BackButton to="/login"/><h1>Forgot Password?</h1><p className="auth-subtitle">Enter your email and we&apos;ll help you reset your password.</p><form onSubmit={submit}><Field name="email" type="email" placeholder="Email address" value={email} onChange={e=>setEmail(e.target.value)} error={error}/><SubmitButton>Continue</SubmitButton></form></AuthStage>}
export function ResetPassword(){const navigate=useNavigate();const[password,setPassword]=useState('');const[error,setError]=useState('');const submit=e=>{e.preventDefault();const next=validatePassword(password);setError(next);if(!next)navigate('/password-reset-success')};return <AuthStage><BackButton to="/forgot-password"/><h1>Reset Password</h1><p className="auth-subtitle">Create a strong new password for your account.</p><form onSubmit={submit}><Field name="password" type="password" placeholder="New password" value={password} onChange={e=>setPassword(e.target.value)} error={error}/><SubmitButton>Reset Password</SubmitButton></form></AuthStage>}
export function PasswordResetSuccess(){return <AuthStage><SuccessMark/><h1>Password Reset</h1><p className="auth-subtitle">Your password has been updated successfully.</p><Link className="primary-button anchor-button" to="/login">Return to Log in</Link></AuthStage>}
